/*

	this.setResult = function(numberAnswer, userletiant){
		 (quiz[numberAnswer - 1].answer == userletiant)
		 ? result[numberAnswer - 1] = true
		 : result[numberAnswer - 1] = false;
	}

	function checkTest() {
		let userletiant = [].indexOf.call(this.parentNode.parentNode.children, this.parentNode)+1;
		let numberAnswer = [].indexOf.call(this.parentNode.parentNode.parentNode.parentNode.children, this.parentNode.parentNode.parentNode)+1;
		let inputs = this.parentNode.parentNode.querySelectorAll('input');
		for(let i = 0; i < inputs.length; ++i){
			inputs[i].checked = false
		};
		self.setResult(numberAnswer, userletiant)
		this.checked=true;
	}

	*/
